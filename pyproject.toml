[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "candlelab-core"
version = "1.0.2"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["candlelab_core*"]
